{
  "name": "ecommerce-admin-panel",
  "version": "1.0.0",
  "description": "Multi-tenant e-commerce admin panel with React frontend and Node.js backend",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && npm install && node server.js",
    "build": "cd backend && npm install",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "install-deps": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": [
    "ecommerce",
    "admin-panel",
    "react",
    "nodejs",
    "multi-tenant",
    "postgresql",
    "jwt-auth"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}